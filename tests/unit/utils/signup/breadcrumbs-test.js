import signupBreadcrumbs from 'houseninja/utils/signup/breadcrumbs';
import { module, test } from 'qunit';

module('Unit | Utility | signup/breadcrumbs', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = signupBreadcrumbs();
    assert.ok(result);
  });
});
