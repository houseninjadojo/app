{{page-title "Home"}}
<View>
    <View::Home
        @user={{this.model.user}}
        @property={{this.model.property}}
        @shouldBookWalkthrough={{this.model.user.shouldBookWalkthrough}}
        @commonRequests={{this.model.commonRequests}}
        @homeCareTips={{this.model.homeCareTips}}
    />
</View>
