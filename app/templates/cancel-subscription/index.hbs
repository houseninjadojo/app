{{page-title "Cancel Subscription"}}
<TopBar @content="focused" @hideHome={{true}} />
<View @hideLogo={{true}} @topBar={{true}}>
    <View::CancelSubscription @model={{this.model}} @topBar={{true}}/>
</View>
