{{page-title "Settings - Security Settings"}}
<Primitive::Modal::Tray @backRoute="settings" @saveAction={{this.saveAction}} @formIsInvalid={{this.formIsInvalid}} @onBack={{this.clearInputs}}>
  <View::AccountSettings::Security @model={{this.model}} @passwords={{this.passwords}} @requirementsModel={{this.requirementsModel}} @validatePasswordRequirement={{this.validatePasswordRequirement}}/>
</Primitive::Modal::Tray>
