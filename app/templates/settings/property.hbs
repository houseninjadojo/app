{{page-title "Settings - Property Information"}}
<Primitive::Modal::Tray @onBack={{this.reset}} @saveAction={{this.saveAction}} @formIsInvalid={{this.formIsInvalid}}>
  <View::AccountSettings::Property @model={{this.propertyInfo}} @fields={{this.fields}} @validateForm={{this.validateForm}}/>
</Primitive::Modal::Tray>
