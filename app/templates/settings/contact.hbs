{{page-title "Settings - Contact Information"}}
<Primitive::Modal::Tray @backRoute="settings" @onBack={{this.resetInputs}} @saveAction={{this.saveAction}} @formIsInvalid={{this.formIsInvalid}}>
  <View::AccountSettings::Contact @fields={{this.fields}} @model={{this.contactInfo}} @validateForm={{this.validateForm}}/>
</Primitive::Modal::Tray>
