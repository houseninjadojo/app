{{page-title "Settings - Contact Information"}}
<Primitive::Modal::Tray @onBack={{this.resetForm}} @saveAction={{this.saveAction}} @formIsInvalid={{this.formIsInvalid}}>
  <View::AccountSettings::Contact @fields={{this.fields}} @model={{this.contactInfo}} @validateForm={{this.validateForm}} @openChatModal={{this.openChatModal}}/>
</Primitive::Modal::Tray>
