<div class="content mb-16">
    <div class="prompt">
        <p class="primary">Payment Method</p>
        <p>Let's get you signed up</p>
        {{#each this.fields as |field|}}
            <Primitive::Input
                {{!-- @field={{field}} --}}
                @id={{field.id}}
                @label={{field.label}}
                @required={{field.required}}
                @type={{field.type}}
                @value={{mut (get this.paymentMethod field.value)}}
            />
        {{/each}}
        <Primitive::Input
            @id={{'promo-code'}}
            @alert={{this.promoCodeAlert}}
            @description={{this.promoCodeDescription}}
            @placeholder={{'Promo Code (optional)'}}
            @label={{'Promo Code'}}
            @value={{mut this.promoCodeInput}}
            @onChange={{(perform this.checkPromoCode)}}
        />
        <div class='terms'>
            <Input
                @type="checkbox"
                @checked={{mut this.agreesToTermsAndConditions}}
                id="terms-agree"
                name="terms"
            />
            <span for="terms">I have read and agree to the <a href="" target="_blank" rel="noopener noreferrer">Terms and Conditions Agreement</a></span>
        </div>
    </div>
    <div class="action">
        <Primitive::Button
            @size='lg' @variant='outline'
            @semantic='primary'
            {{on 'click' this.goBack}}
        >
            Previous
        </Primitive::Button>
        <Primitive::Button
            @size='lg'
            @variant='solid'
            @semantic='primary'
            {{on 'click' this.savePaymentMethod}}
        >
            Subscribe
        </Primitive::Button>
    </div>
</div>