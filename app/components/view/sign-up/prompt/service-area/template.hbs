<div class="content mb-4">
    <HouseNinja::ReferralCode />
    <div class="hero mb-16">
        <img src="/assets/images/wordmark+vertical+black+teal.svg" class="view-logo" alt="house ninja"/>
        <p>To get started, enter the zip code of your property.</p>
    </div>
    <div class="prompt">
        <p class="primary">What is your zip code?</p>
        <Primitive::Input
            @label='Zipcode'
            @placeholder='Zipcode'
            @hideLabel={{true}}
            @required={{true}}
            @type={{'number'}}
            @value={{mut this.zipcode}}
            @onInput={{this.validateForm}}
        />
    </div>
    <div class="action single">
        <Primitive::Button
          @size='lg'
          @variant='solid'
          @semantic='primary'
          @disabled={{this.formIsInvalid}}
          {{on 'click' this.checkServiceArea}}
        >
            Next <img src="/assets/icons/right-arrow.svg" alt="right" class="icon"/>
        </Primitive::Button>
    </div>
</div>