<div class="settings-view dark">
    <h1 class="hn view-title">Change Payment Method</h1>

    {{#if (get @model 'cardNumber')}}    
        <div class="mb-16">
            <p class="hn label">Current Payment Information</p>
            <p>{{@model.obfuscated.brand}} ending in {{@model.obfuscated.lastFour}}</p>
        </div>
    {{/if}}

    {{#each this.paymentFields as |field|}}
        <Primitive::Input
          @id={{field.id}}
          @required={{field.required}}
          @placeholder={{field.placeholder}}
          @value={{mut (get @model.obfuscated field.id)}}
          @description={{field.description}}
          @label={{field.label}}
          @type={{field.type}}
          @disabled={{field.disabled}}
        />
    {{/each}}
</div>