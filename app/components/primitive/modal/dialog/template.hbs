<div class="hn modal {{unless @isVisible 'hide'}}">
    {{#if @disableClose}}      
      <div class="hn overlay" />
    {{else}}
      <Primitive::Button @variant="plain" {{on "click" this.toggleModal}}>
          <div class="hn overlay" />
      </Primitive::Button>
    {{/if}}
    <div class="hn modal-container dialog light">            
        <div class="body">
          {{yield}}
        </div>        
    </div>
</div>