<nav class="hn nav-bar">
  <Primitive::Button @type="button" @size="md" @variant="solid" @semantic="accent" {{on "click" this.openChatModal}}>
        Help me with...
  </Primitive::Button>
  <ul class="nav-menu">
    <li class="nav-menu-item">
      <Primitive::Button {{on "click" (fn this.selectRoute 'dashboard.home')}} @variant="plain" @class={{this.isHomeActive}}>
        <img src="/assets/icons/property.svg" alt="home icon" class="nav-menu-item-icon"/>
        Home
      </Primitive::Button>
    </li>
    <li class="nav-menu-item">
      <Primitive::Button  {{on "click" (fn this.selectRoute 'dashboard.handle-it')}} @variant="plain" @class={{this.isHandleItActive}}>
        <img src="/assets/icons/tools.svg" alt="handle it icon" class="nav-menu-item-icon"/>
        Handle It
      </Primitive::Button>
    </li>
    <li class="nav-menu-item">
      <Primitive::Button  {{on "click" (fn this.selectRoute 'dashboard.work-history')}} @variant="plain" @class={{this.isWorkHistoryActive}}>      
        <img src="/assets/icons/activity.svg" alt="history icon" class="nav-menu-item-icon"/>
        History
      </Primitive::Button>
    </li>
  </ul>
</nav>