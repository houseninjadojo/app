<div class="settings-view">
    <h1 class="view-title">Edit Contact Information</h1>
    {{#each this.contactFields as |field|}}            
        <Primitive::Input @field={{field}} required={{field}} @warn={{false}} />
    {{/each}}
</div>