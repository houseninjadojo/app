<Primitive::RecordList
  @showTimestamp={{true}}
  @showIcon={{false}}
  @records={{@records}}
  @onRecordClick={{@onRecordClick}}
  @ifEmpty={{hash
    message='You have no service activity'
    action='Request Service'
  }}
/>